//https://developer.mozilla.org/pt-BR/docs/Web/JavaScript/Reference/Global_Objects/Date

/*Resumo
Cria uma instância JavaScript de  Date que representa um único momento no tempo. Objetos Data são baseados no valor de tempo que é o número de milisegundos desde 1º de Janeiro de 1970 (UTC).*/

//Construtor
new Date();
new Date(valor);
new Date(dataString);
new Date(ano, mês, dia, hora, minuto, segundo, milissegundo);